import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#2f4f4e',
    },
    secondary: {
      main: '#f0e78c',
    },
    success: {
      main: '#f0f2f5',
    },
    warning: {
      main: '#d32f2f',
    },
    info: {
      main: '#0288d1',
    },
  },
});

export default theme;
